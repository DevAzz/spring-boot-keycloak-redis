apiVersion: v1
kind: Service
metadata:
  name: {{ include "fullname" . }}
spec:
  selector:
    app: {{ include "fullname" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8081
