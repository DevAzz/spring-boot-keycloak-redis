apiVersion: v1
kind: Service
metadata:
  name: java-app-2
spec:
  selector:
    app: java-app
    replica-id: "2"
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8081
